FROM node:lts-alpine
WORKDIR /app

# Install production dependencies only
COPY package*.json ./
RUN npm install --production --silent

# Copy source code
COPY . .

# Match the port used in index.js and docker-compose
EXPOSE 3000

CMD ["node", "src/index.js"]
