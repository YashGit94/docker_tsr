location /api/ {
    resolver 8.8.8.8;
    # Update this with the NEW backend URL from your Cloud Run console
    proxy_pass https://backend-service-test-xxxxx-uc.a.run.app;
    proxy_set_header Host $proxy_host;
    proxy_set_header X-Real-IP $remote_addr;
}
